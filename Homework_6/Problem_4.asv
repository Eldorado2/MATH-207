%%%%% MATH 207 Homework #6 %%%%%

% Problem 4
% Compute the adjugate of the given​ matrix, 
% and then use the Inverse Formula to give the inverse of the matrix.

A = [1 2 3; ...
    -2 3 -1; ...
    0 2 4];

% Calculate adjugate
adj_A = adjoint(A);
% Calculate inverse
inv_A = inv(A);

% Find using cofactor matrix
C = zeros(3);
for i = 1:3
    for j = 1:3
        C(i,j) = (-1)^(i+j)*det(A())
    end
end